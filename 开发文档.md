# 舆情分析平台 · 开发文档

> 本文档为《舆情分析平台》的**开发实施蓝图**，严格以需求文档 2.2.1~2.2.12 为准，同时吸收 RAG 链路（主题检索 → 证据召回 → 报告生成）的技术增强。
> 适用读者：后端 / 前端 / 算法 / 测试 / 运维工程师。

---

## 目录

1. [项目概述](#1-项目概述)
2. [需求范围映射](#2-需求范围映射)
3. [系统架构](#3-系统架构)
4. [技术栈](#4-技术栈)
5. [项目目录结构](#5-项目目录结构)
6. [模块设计](#6-模块设计)
7. [状态机定义](#7-状态机定义)
8. [数据存储规范](#8-数据存储规范)
9. [REST API 规范](#9-rest-api-规范)
10. [实时通信协议](#10-实时通信协议)
11. [报告输出格式](#11-报告输出格式)
12. [YAML 工作流引擎](#12-yaml-工作流引擎)
13. [前端页面设计](#13-前端页面设计)
14. [测试策略](#14-测试策略)
15. [部署方案](#15-部署方案)
16. [验收标准与演示清单](#16-验收标准与演示清单)

---

## 1. 项目概述

### 1.1 目标
面向指定主题的舆情分析平台，从**公开信息、多媒体内容、内部沉淀数据**三个方向并行采集，汇总生成结构化舆情报告，并提供可运行的 Web 控制台。

### 1.2 交付形式
- Web 控制台（前端 SPA）
- 后端分析服务（API + RAG 工作流引擎）
- 配置文件（YAML 工作流 + config.json）
- 示例数据 + 至少一个完整主题分析案例
- 接口说明 + 开发文档（本文档）+ Docker 部署编排

### 1.3 核心逻辑链（RAG 链路）
```
主题关键词
   │
   ▼
① 向量检索 (BGE-M3 encode)  ──→ 候选证据集
   │
   ▼
② 重排序 (BGE-Reranker-large) ──→ 高相关证据
   │
   ▼
③ LLM 分析 (Qwen2.5-72B) ──→ 结构化报告 + 图谱 JSON
   │
   ▼
④ 保存 (report.md / graph.json) ──→ 可溯源回查
```
该链路由 **YAML 配置文件驱动**，支持步骤编排、条件分支、重试与热更新。

---

## 2. 需求范围映射

| 需求章节 | 内容 | 开发落地 |
|---|---|---|
| 2.2.1 开发范围 | 控制台/分析/展示能力 | 后端 6 大模块 + 前端 6 页面 |
| 2.2.2 用户角色 | 操作用户/报告查看人/系统管理员 | RBAC 优先项（二期可做，首期以 `role` 字段预留） |
| 2.2.3 页面需求 | 6 个核心页面 | 见 [第 13 章](#13-前端页面设计) |
| 2.2.4 任务状态与执行流程 | 三套状态机 | 见 [第 7 章](#7-状态机定义) |
| 2.2.5 前端模块要求 | 6 个功能模块 | 见 [第 13 章](#13-前端页面设计) |
| 2.2.6 后端模块要求 | 6 大后端模块 | 见 [第 6 章](#6-模块设计) |
| 2.2.7 数据存储要求 | 目录/文件规范 | 见 [第 8 章](#8-数据存储规范) |
| 2.2.8 采集与分析规则 | 来源类型/溯源 | 见 [第 8.6 章](#86-采集与分析规则) |
| 2.2.9 接口要求 | 20 个端点 | 见 [第 9 章](#9-rest-api-规范) |
| 2.2.10 实时消息类型 | 6 类消息 | 见 [第 10 章](#10-实时通信协议) |
| 2.2.11 报告输出格式 | 8 个章节 | 见 [第 11 章](#11-报告输出格式) |
| 2.2.12 验收标准 | 功能验收 | 见 [第 16 章](#16-验收标准与演示清单) |

---

## 3. 系统架构

```
┌────────────────────────────────────────────────────┐
│                    前端 SPA (React/Vue)               │
│  控制台 │ 检索 │ 论坛 │ 图谱 │ 配置 │ 系统状态         │
└───────────────┬────────────────────────┬────────────┘
                │ REST + WebSocket       │
┌───────────────▼────────────────────────▼────────────┐
│              FastAPI 后端服务 (控制层)                 │
│  ┌─────────┬─────────┬─────────┬─────────┐           │
│  │process  │ search  │ forum   │ graph    │           │
│  │manager  │ analysis│ module  │ module   │           │
│  └────┬────┴────┬────┴────┬────┴────┬────┘           │
│       │         │Wf引擎   │         │                │
│  ┌────▼─────────▼─────────▼─────────▼────┐           │
│  │       YAML 工作流引擎 (DAG 编排)          │           │
│  └──────────────────┬─────────────────────┘           │
└─────────────────────┼─────────────────────────────────┘
                      │ 进程/子应用 + 模型服务
     ┌────────────────┼──────────────────┐
     ▼                ▼                  ▼
┌────────────┐ ┌──────────────┐    ┌──────────────────┐
│ 单功能应用  │ │ 论坛采集器    │    │ 模型服务(可进程池)│
│ topic/多媒 │ │ (子进程)      │    │ BGE-M3 / Reranker│
│ 体/洞察    │ └──────────────┘    │ / Qwen2.5-72B    │
└────────────┘                     └──────────────────┘
```

---

## 4. 技术栈

### 4.1 后端
| 技术 | 用途 |
|---|---|
| Python 3.10+ | 主语言 |
| FastAPI + Uvicorn | Web 框架 / ASGI |
| Pydantic | 数据校验 |
| PyYAML | 工作流配置解析 |
| websockets | 实时推送 |
| httpx | 异步 HTTP / 模型调用 |
| transformers + sentence-transformers + torch | BGE-M3 / BGE-Reranker |
| onnxruntime / TensorRT（可选） | 模型加速，显存 ≤ 8GB |
| pytest | 单元/集成测试 |
| prometheus-fastapi-instrumentator | 指标监控 `/metrics` |

### 4.2 前端
| 技术 | 用途 |
|---|---|
| React 或 Vue 3 | SPA 框架 |
| Vite | 构建工具 |
| ECharts / D3.js | 图谱可视化 |
| 原生 WebSocket 或 socket.io / 原生 WS | 实时消息 |

### 4.3 模型
| 模型 | 角色 | 说明 |
|---|---|---|
| BGE-M3 | 向量化检索 | 多语言、跨模态语义检索 |
| BGE-Reranker-large | 精排 | 交叉编码器重排序 |
| Qwen2.5-72B-Instruct | 分析/生成 | 观点提取、情绪分析、时间线、报告撰写 |

---

## 5. 项目目录结构

```
opineye/
├── src/
│   ├── api/                  # 控制层：路由、渲染、状态接口
│   │   ├── api_routes.py     # 20 个端点 + 新增 analyze 任务接口
│   │   ├── page_routes.py    # /graph-viewer 页面路由
│   │   └── ws_routes.py      # WebSocket 端点 /ws
│   ├── core/
│   │   ├── model_loader.py   # 单例：BGE-M3 / Reranker 懒加载 + encode/rerank
│   │   ├── workflow_engine.py# YAML → DAG 执行引擎
│   │   └── llm_client.py     # Qwen2.5-72B 封装
│   ├── modules/
│   │   ├── process_manager.py# 单功能应用启停/状态/输出采集
│   │   ├── analysis_service.py# analyze()/get_report()/get_graph()
│   │   ├── forum_module.py   # 论坛采集任务控制 + 日志
│   │   ├── graph_module.py   # 图谱加载/按报告查询/关系检索
│   │   └── config_module.py  # 配置读取/保存/热更新
│   ├── storage/
│   │   └── file_store.py     # 2.2.7 目录读写类
│   ├── schemas/              # Pydantic 模型（API 出入参 / WS 消息）
│   └── app.py                # FastAPI 入口 + 生命周期 + Prometheus
├── config/
│   ├── config.json           # 系统配置、运行参数、默认应用开关
│   └── workflows/
│       └── opinion_analysis.yaml  # 默认舆情分析工作流
├── tests/
│   ├── unit/
│   ├── integration/
│   └── fixtures/
├── docker/
│   ├── Dockerfile.api
│   ├── Dockerfile.frontend
│   └── Dockerfile.model
├── frontend/                 # React/Vue SPA
│   └── src/pages/
├── scripts/
│   └── demo.sh               # 一键完整主题分析演示
├── data/
│   ├── corpus/               # 示例语料
│   └── sample_topic.py       # 示例主题（特斯拉股价波动）
├── requirements.txt
├── pyproject.toml
├── docker-compose.yml
└── README.md
```

---

## 6. 模块设计

### 6.1 控制层模块（`src/api/`）
- 负责 Web 路由、页面渲染、状态接口、系统启停入口。
- 注入工作流引擎、进程管理器、论坛/图谱/配置模块。
- 统一异常处理与鉴权（可选，预留 `Authorization` 中间件）。

### 6.2 进程管理模块（`process_manager.py`）
- 单功能应用（主题检索、多媒体检索、洞察分析、论坛采集等）的启动 / 停止 / 状态采集 / 输出采集。
- 每个应用**独立保存**运行状态、`stdout`、`stderr`。
- 停止后不再接收新任务，但**保留**已生成输出与日志。
- 异步进程池 + 回调，支持 ≥10 任务并发。

### 6.3 检索分析模块（`analysis_service.py`）
- `analyze(query, source_types) -> report_id`：驱动工作流引擎执行 RAG 链路。
- `get_report(report_id)`：读取 `reports/{report_id}/report.md`。
- `get_graph(report_id)`：读取 `graphs/{report_id}/graph.json`。
- 工作流执行期间推送 `progress` 消息（`retrieving` / `reranking` / `generating`）。

### 6.4 论坛模块（`forum_module.py`）
- 论坛采集任务启停、日志输出、历史记录保存。
- 每次任务落 `runtime/forum/latest.log`，并按日归档 `runtime/forum/history/{date}.json`。

### 6.5 图谱模块（`graph_module.py`）
- 最新图谱加载、指定报告图谱查询、按节点/关系条件检索。
- 图谱结果至少含：节点标识、节点类型、关系类型、来源引用。

### 6.6 配置模块（`config_module.py`）
- 配置读取、保存、运行参数热更新（写 `config.json`，触发工作权重载）。

---

## 7. 状态机定义

### 7.1 单功能应用状态（2.2.4）
```
stopped ──start──▶ starting ──▶ running ──stop──▶ stopping ──▶ stopped
                        │                           │
                        └──▶ failed ◀──(任何异常)────┘
```

### 7.2 论坛采集状态
```
idle ──start──▶ running ──stop──▶ stopped ──▶ (再启 idle)
      │           │
      └──▶failed  └──▶ failed
```

### 7.3 系统状态
```
offline ──start──▶ starting ──▶ online ──shutdown──▶ shutting_down ──▶ offline
```
系统启动后，允许**独立**启动主题检索、多媒体检索、洞察分析等单功能应用。

---

## 8. 数据存储规范

### 8.1 目录结构（严格对应 2.2.7）
| 路径 | 内容 |
|---|---|
| `config.json` | 系统配置、运行参数、默认应用开关 |
| `runtime/apps/{app_name}.log` | 单功能应用标准输出 + 错误输出 |
| `runtime/forum/latest.log` | 论坛采集最新运行日志 |
| `runtime/forum/history/{date}.json` | 论坛日志历史 |
| `reports/{report_id}/report.md` | 结构化分析报告 |
| `graphs/{report_id}/graph.json` | 图谱节点/边 |
| `outputs/{app_name}/latest.txt` | 单功能应用最近一次文本输出 |

### 8.2 扩展目录（RAG 增强，可选）
| 路径 | 内容 |
|---|---|
| `data/embeddings/` | 向量缓存（BGE-M3 产物） |
| `data/cache/` | LLM 结果缓存（按 query 归一化 key） |

### 8.3 检索记录
每次主题检索**至少保存**：主题词、执行时间、来源摘要、分析输出。

### 8.4 图谱记录
图谱结果至少保存：节点标识、节点类型、关系类型、来源引用。

### 8.5 论坛日志
论坛日志至少保留：时间、事件类型、消息内容、任务状态。

### 8.6 采集与分析规则（2.2.8）
- **来源类型（`source_types`）**：至少支持 `news`、`image`、`video`、`forum_post`、`internal_data`。
- **可溯源**：最终报告中的结论必须能追溯到来源内容、论坛记录或图谱节点。

---

## 9. REST API 规范

> 需求 2.2.9 共 **20 个端点** = 18 个数据接口 + 2 个页面路由。

### 9.1 系统控制类
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/status` | 查询控制台状态和各应用运行状态 |
| GET | `/api/system/status` | 查询整体服务状态 |
| POST | `/api/system/start` | 启动整体服务 |
| POST | `/api/system/shutdown` | 关闭整体服务 |

### 9.2 单功能应用类
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/start/{app_name}` | 启动指定单功能应用 |
| GET | `/api/stop/{app_name}` | 停止指定单功能应用 |
| GET | `/api/output/{app_name}` | 查询指定应用最近输出 |
| GET | `/api/test_log/{app_name}` | 查询指定应用测试日志 |

### 9.3 检索分析类
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/search` | 字段 `query`、`source_types`，返回检索+分析结果 |

### 9.4 论坛类
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/forum/start` | 启动论坛采集 |
| GET | `/api/forum/stop` | 停止论坛采集 |
| GET | `/api/forum/log` | 查询论坛最新日志 |
| POST | `/api/forum/log/history` | 按日期查询论坛历史日志 |

### 9.5 配置类
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/config` | 查询系统配置 |
| POST | `/api/config` | 更新系统配置 |

### 9.6 图谱类
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/graph/latest` | 查询最新图谱结果 |
| GET | `/api/graph/{report_id}` | 查询指定报告图谱 |
| POST | `/api/graph/query` | 按节点或关系条件查询图谱 |

### 9.7 页面路由
| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/graph-viewer` | 进入图谱查看页 |
| GET | `/graph-viewer/{report_id}` | 进入指定报告图谱查看页 |

### 9.8 扩展任务型 API（RAG 增强，新增）
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/analyze` | 异步提交分析任务，返回 `task_id` |
| GET | `/api/analyze/{task_id}/status` | 查询任务进度（WebSocket 推送同步） |
| GET | `/api/analyze/{task_id}/result` | 获取最终报告 |
| WS | `/ws` | 实时消息通道（见 [第 10 章](#10-实时通信协议)） |

---

## 10. 实时通信协议

### 10.1 标准消息类型（2.2.10，共 6 类）
| 类型 | 必含字段 | 说明 |
|---|---|---|
| `app_status` | `app_name`, `status` | 应用状态变更 |
| `app_output` | `app_name`, `output_text` | 应用输出 |
| `forum_log` | `message_text`, `task_status` | 论坛日志 |
| `system_status` | `system_status`, `running_apps` | 系统状态 |
| `graph_ready` | `report_id`, `graph_path` | 图谱就绪 |
| `error` | `module_name`, `error_message` | 错误 |

### 10.2 扩展消息类型（新增 1 类）
| 类型 | 必含字段 | 说明 |
|---|---|---|
| `progress` | `task_id`, `stage`, `progress`, `message` | 工作流执行进度（`retrieving`/`reranking`/`generating`） |

> **合计 7 种消息类型**（6 标准 + 1 扩展）。

### 10.3 消息帧示例
```json
{ "type": "progress", "task_id": "t-123", "stage": "reranking", "progress": 0.6, "message": "精排证据中…" }
```

---

## 11. 报告输出格式

> 严格对应 2.2.11，共 **8 个章节**（注意：比原始提示词模板多「传播渠道」）。

| 章节 | 内容要求 |
|---|---|
| **事件概述** | 说明主题背景和分析范围 |
| **时间线** | 按时间顺序列出关键事件 |
| **传播渠道** | 按来源类型（news/image/video/forum_post/internal_data）统计传播分布 |
| **主要观点** | 列出不同来源和不同角色总结出的核心观点 |
| **情绪倾向** | 给出正向、中性、负向的整体判断 |
| **风险判断** | 列出高风险点和关注事项 |
| **重点证据** | 列出来源标题、链接、摘要和引用位置 |
| **结论摘要** | 给出最终结论和建议 |

### 11.1 结论可溯源
每个章节（尤其「重点证据」「结论摘要」）必须携带**来源引用**，指向来源内容、论坛记录或图谱节点，实现回查。

---

## 12. YAML 工作流引擎

### 12.1 设计原则
- 解析 YAML → 构建**任务 DAG**。
- 支持：步骤间依赖、条件分支、重试策略（`retry`）、超时控制、热更新（重载配置即时生效）。
- 集成日志与指标埋点：执行时长、成功/失败状态、各步骤耗时。

### 12.2 默认工作流 `config/workflows/opinion_analysis.yaml`
```yaml
workflow:
  name: "舆情主题分析"
  version: "1.0"
  input:
    - query: string
    - source_types: list   # news/image/video/forum_post/internal_data
  steps:
    - id: retrieve
      type: vector_search
      engine: bge-m3
      params:
        top_k: 20
        threshold: 0.7
        index_name: "public_opinion"
      retry: 2

    - id: rerank
      type: cross_encoder
      engine: bge-reranker-large
      params:
        top_n: 5
        min_score: 0.4
      depends_on: retrieve
      retry: 2
      condition: "{{retrieve.results|length}} > 0"

    - id: llm_analyze
      type: llm_generate
      model: "Qwen2.5-72B-Instruct"
      prompt_template: |
        你是一位专业的舆情分析师。请根据以下检索到的上下文（已按相关性排序），生成一份结构化的分析报告。
        报告必须包含以下章节（共 8 节）：
        1. 事件概述：说明主题背景和分析范围。
        2. 时间线：按时间顺序列出关键事件。
        3. 传播渠道：按来源类型统计传播分布。
        4. 主要观点：列出不同来源和不同角色总结出的核心观点。
        5. 情绪倾向：给出正向、中性、负向的整体判断。
        6. 风险判断：列出高风险点和关注事项。
        7. 重点证据：列出来源标题、链接、摘要和引用位置（须可溯源，至少 3 条）。
        8. 结论摘要：给出最终结论和建议。

        上下文：
        {{context}}

        用户问题：{{query}}
      params:
        temperature: 0.3
        max_tokens: 2000
      depends_on: rerank
      retry: 1

    - id: save_report
      type: file_writer
      input: "{{llm_analyze.output}}"
      output_path: "reports/{{report_id}}/report.md"
      depends_on: llm_analyze

    - id: build_graph
      type: graph_builder
      extract_entities: true
      extract_relations: true
      input: "{{llm_analyze.output}}"
      output_path: "graphs/{{report_id}}/graph.json"
      depends_on: llm_analyze
```

### 12.3 引擎要求
- `condition` 支持 Jinja 表达式，可访问上游步骤输出。
- `retry` 指数退避重试；每步骤独立超时。
- 工作流 ID 对应 `report_id`，供图谱/报告回查。

---

## 13. 前端页面设计

### 13.1 页面需求（2.2.3，共 6 页）
| 页面 | 功能 |
|---|---|
| 控制台首页 | 各单功能应用状态、启动/停止按钮、最近输出摘要、系统状态 |
| 主题检索页 | 输入主题词/关键词/检索条件，发起分析并查看文本输出；**可展开查看重排后中间证据段落（溯源）** |
| 论坛监控页 | 论坛采集启动状态、最新日志、历史日志查询入口 |
| 图谱查看页 | 最新图谱 + 指定报告图谱，支持节点查询和关系查看 |
| 配置管理页 | 系统配置、采集配置、运行参数展示，支持读取与保存 |
| 系统状态页 | 整体服务状态、各应用状态、最近错误信息、资源运行概况 |

### 13.2 前端模块（2.2.5，共 6 模块）
| 模块 | 职责 |
|---|---|
| 控制台模块 | 应用状态查询、应用启停、系统状态展示 |
| 输出模块 | 单功能应用输出查看、测试日志查看、滚动展示 |
| 论坛模块 | 论坛任务启停、论坛日志查看、历史日志查询 |
| 图谱模块 | 图谱查看、报告切换、图查询结果展示 |
| 检索模块 | 主题检索请求、结果展示、来源查看 |
| 配置模块 | 配置读取、更新、回显 |

### 13.3 关键交互
- **检索页**：展示最终报告的同时，必须能**展开查看重排序后的中间证据段落**，实现溯源。
- **图谱页**：集成 ECharts/D3.js，支持节点查询、关系查看、报告切换。

---

## 14. 测试策略

### 14.1 单元测试
- 核心检索（BGE-M3 encode）、重排序（Reranker）逻辑。
- 工作流引擎：YAML 解析、DAG 构建、条件分支、重试、超时。

### 14.2 集成测试
- 模拟完整主题分析（如「特斯拉股价波动」）。
- 验证最终报告包含 2.2.11 的全部 8 个章节，且结论可溯源（证据/图谱节点引用完整）。

### 14.3 性能测试
- 单次完整分析（检索+重排+生成）总耗时 ≤ **8 秒**。
- 纯向量检索 ≤ **1 秒**。
- ≥ 10 个分析任务并发执行（异步 IO / 进程池）。
- 模型加速后显存 ≤ **8GB**（ONNX / TensorRT）。

---

## 15. 部署方案

### 15.1 Docker 镜像
- `Dockerfile.api`：API 服务（FastAPI + Uvicorn + Pydantic）。
- `Dockerfile.frontend`：Nginx 托管 SPA 静态资源 + 反向代理 /api 与 /ws。
- `Dockerfile.model`：模型服务（BGE-M3 / Reranker / Qwen2.5-72B）。

### 15.2 docker-compose 编排
- 服务：`api`、`frontend`、`model`、`prometheus`（可选）。
- GPU 资源：`runtime: nvidia`，声明 `gpus: all` 或限制显存。
- 健康检查：`/api/status`、`/healthz`（各服务）。
- **挂载**：模型文件目录 + 项目目录（`config/`、`data/`、`reports/`、`graphs/`、`runtime/`），实现配置热更新与持久化。
- 启动顺序：`model` → `api` → `frontend`，依赖 `depends_on: condition: service_healthy`。

### 15.3 配置热更新
- `POST /api/config` 落盘 `config.json` → 通知工作流引擎与各模块重载，实现**即时生效**。

---

## 16. 验收标准与演示清单

### 16.1 功能验收（对照 2.2.12）
- [ ] 能通过控制台**启动和停止**单功能应用
- [ ] 能查看**应用状态**、**应用输出**和**最近错误信息**
- [ ] 能**启动论坛采集**并查看**最新日志**和**历史日志**
- [ ] 能发起**主题检索**并看到**结构化分析结果**
- [ ] 能查看**最新图谱**和**指定报告图谱**
- [ ] 能**更新配置**并**即时生效**
- [ ] 能完成**至少一个完整主题**的检索、分析、图谱查看和结果展示演示

### 16.2 交付物检查清单
- [ ] 全部 20 个端点（18 数据接口 + 2 页面路由）已实现且功能正确
- [ ] 新增 `analyze` 任务型 API + `progress` 消息，WebSocket 共推送 7 种消息类型
- [ ] 前端 6 个页面可正常访问，且与后端数据联动
- [ ] 报告含 8 个章节（含「传播渠道」），结论可溯源
- [ ] `docker-compose up -d` 一键启动完整系统
- [ ] 提供 `demo.sh`：自动执行一个完整主题分析并打开报告

### 16.3 演示步骤（demo.sh 流程建议）
1. `docker-compose up -d` → 系统进入 `online`。
2. 控制台首页确认各应用 `running`。
3. 启动论坛采集 → 查看 `latest.log` 与历史日志。
4. 主题检索页输入「特斯拉股价波动」，选择 `source_types`（news/video/forum_post 等）。
5. 观察 `progress`（retrieving → reranking → generating）。
6. 打开生成的 `report.md`，核对 8 章节并溯源「重点证据」。
7. 图谱页查看 `graph.json`，按节点/关系查询并切换报告。
8. 修改配置 → 验证即时生效。

---

_本开发文档由需求文档 2.2 与 RAG 增强设计综合生成，开发过程中若需求变更，请先行更新本文档再修改代码。_
