# 舆情分析平台 · 开发任务清单

> 版本：v1.0　|　创建日期：2026-08-26
> 依据：《开发文档.md》第 17 节开发顺序建议 + 第 4 节目录结构
> 里程碑：M1–M5，每个任务展开到实现级步骤，含依赖关系与验收点

---

## 总览

| 里程碑 | 内容 | 产出 | 前置依赖 |
| ------ | ---- | ---- | -------- |
| M1 | 后端骨架 + 配置/进程管理 + 状态接口 | 可跑通启停的最小后端 | — |
| M2 | 检索/分析/报告/图谱服务 + 剩余 REST 接口 | 后端全量 API | M1 |
| M3 | WebSocket 实时通道（后端广播 + 前端封装） | 实时状态/日志流 | M2 |
| M4 | 前端 6 页面 + ECharts 图谱 + Pinia | 完整控制台 | M1、M3 |
| M5 | 示例数据 + 完整案例 + API 文档 + 测试用例 | 满足全部验收 | M2、M4 |

---

## M1 — 后端骨架与状态接口（最小可跑通）

### 任务 1.1　初始化项目目录结构

**目标**：创建与开发文档第 4 节一致的目录树。

**实现步骤**：
1. 在项目根下创建 `backend/`、`frontend/`、`docs/` 三个顶层目录。
2. `backend/` 下创建 `app/`、`apps/`、`data/`、`examples/`、`tests/`。
3. `app/` 下创建 `models/`、`core/`、`api/`、`services/` 四个子目录。
4. `data/` 下预建 `runtime/apps/`、`runtime/forum/history/`、`reports/`、`graphs/`、`outputs/`（空目录，后续由 `store.py` 兜底创建）。
5. `backend/` 下创建 `requirements.txt`、`run.py`。
6. 每个 Python 包目录下创建 `__init__.py`（`app/`、`models/`、`core/`、`api/`、`services/`、`apps/`、`tests/`）。

**产出**：完整目录树 + `requirements.txt` + 各 `__init__.py`。

**验收点**：目录结构与文档 4 节一致，无缺失。

---

### 任务 1.2　数据目录初始化 + 默认 config.json 模板

**目标**：服务首次启动时自动创建必要子目录并写入默认配置。

**实现步骤**：
1. 在 `settings.py` 定义路径常量：`DATA_ROOT`、`BACKEND_DIR`、`APPS_DIR`、`CONFIG_PATH`、`RUNTIME_APPS_DIR`、`RUNTIME_FORUM_DIR`、`REPORTS_DIR`、`GRAPHS_DIR`、`OUTPUTS_DIR`。
2. 在 `store.py` 实现 `ensure_data_dirs()`：检查并创建 `runtime/apps/`、`runtime/forum/history/`、`reports/`、`graphs/`、`outputs/`。
3. 在 `store.py` 实现 `DEFAULT_CONFIG` 常量（内容与文档 6.1 完全一致）。
4. 若 `config.json` 不存在，调用 `write_json(CONFIG_PATH, DEFAULT_CONFIG)` 写入。
5. 在 `main.py` 应用启动事件中调用 `ensure_data_dirs()` 与配置初始化。

**产出**：`settings.py` 路径常量、`store.py` 初始化函数、默认配置模板。

**验收点**：删除 `data/` 后重启服务，目录与 `config.json` 自动恢复。

---

### 任务 1.3　全局配置加载（损坏回退）

**目标**：加载 `config.json`，损坏时回退默认值且不崩溃。

**实现步骤**：
1. 在 `config.py` 实现 `load_config()`：读取 `CONFIG_PATH`。
2. 用 `try/except` 包裹 JSON 解析，失败时记录 `logger.warning` 并返回 `DEFAULT_CONFIG` 的深拷贝。
3. 实现 `get_config()` 单例访问器（进程内缓存，`POST /api/config` 更新后刷新缓存）。
4. 校验关键字段：`system.default_apps`、`collection.default_source_types` 等缺失时用默认值补齐。

**产出**：`config.py` 配置加载模块。

**验收点**：手写损坏的 `config.json` 后启动，服务正常且使用默认配置。

---

### 任务 1.4　ProcessManager 子进程生命周期

**目标**：单功能应用以子进程运行，支持启停、状态采集、输出采集。

**实现步骤**：
1. 定义 `AppProc` 上下文类（`proc`/`status`/`log_f`/`pump_task`，用 `__slots__`）。
2. 实现 `_launch(app_name)`：`asyncio.create_subprocess_exec`，`stderr=STDOUT` 合并，`cwd=BACKEND_DIR`。
3. 实现 `_pump()` 协程：逐行读取 `proc.stdout`，写入日志文件、flush、广播 `app_output`；`finally` 中关闭 `log_f`、`await proc.wait()`、按 `returncode` 判定 `failed`/`stopped`，退出码为 0 时写 `latest.txt`。
4. 实现 `start(app_name)`：状态为 `stopped`/`failed` 时置 `starting` → `running`。
5. 实现 `stop(app_name)`：置 `stopping`，Windows 用 `proc.terminate()`、POSIX 用 `send_signal(SIGTERM)`，等待最多 5s 未退则 `proc.kill()`，置 `stopped`。
6. 实现 `status()`、`all_status()`、`read_output()`、`read_log(tail)`。
7. 重复启动检测：非 `stopped`/`failed` 状态拒绝启动。

**产出**：`core/process_manager.py` 完整实现。

**验收点**：子进程状态机 `stopped→starting→running→stopping→stopped` 流转正确，异常退出置 `failed`。

---

### 任务 1.5　文件安全读写

**目标**：JSON 原子写 + 日志追加加锁，防止并发损坏。

**实现步骤**：
1. 实现 `read_json(path)`：读取并解析，不存在返回 `None` 或默认值。
2. 实现 `write_json(path, data)`：先写临时文件 `path.tmp`，再 `os.replace()` 原子替换。
3. 引入 `filelock`，对 JSON 写入与日志追加加跨进程锁（或 `asyncio.Lock` 进程内锁，二选一，文档建议 filelock）。
4. 实现 `append_log(path, line)`：加锁后以追加模式写入并 flush。
5. 路径安全：所有操作基于 `DATA_ROOT` 拼接后 `resolve()`，校验仍在 `DATA_ROOT` 内。

**产出**：`core/store.py` 读写工具。

**验收点**：并发写入 JSON 不损坏，日志追加无交叉。

---

### 任务 1.6　状态接口

**目标**：暴露应用启停、状态、输出、日志接口。

**实现步骤**：
1. 实现 `GET /api/status`：返回 `{ system_status, apps: {name: status} }`。
2. 实现 `GET /api/start/{app_name}`：调用 `ProcessManager.start`，返回 `{ app_name, status }`。
3. 实现 `GET /api/stop/{app_name}`：调用 `ProcessManager.stop`。
4. 实现 `GET /api/output/{app_name}`：返回 `{ app_name, output_text }`。
5. 实现 `GET /api/test_log/{app_name}`：支持 `?tail=200`，返回 `{ app_name, lines: [...] }`。
6. `app_name` 用正则 `^[a-z_]+$` 校验，非法返回 422/400。
7. 统一响应结构 `{ code, message, data }`（封装 `success_response` / `error_response` 辅助函数）。

**产出**：`api/routes_status.py`。

**验收点**：接口可启停应用、查状态/输出/日志，非法 app_name 被拦截。

---

### 任务 1.7　系统接口

**目标**：整体服务状态查询与启停。

**实现步骤**：
1. 实现 `GET /api/system/status`：返回 `{ system_status, running_apps, errors }`。
2. 实现 `POST /api/system/start`：启动整体服务（默认启动 `config.system.default_apps` 中的应用）。
3. 实现 `POST /api/system/shutdown`：停止全部运行中应用，置系统状态 `shutting_down` → `offline`。
4. 维护系统状态机 `offline/starting/online/shutting_down`。

**产出**：`api/routes_system.py`。

**验收点**：整体启停正确，系统状态流转符合文档 5.3。

---

### 任务 1.8　全局异常中间件 + 安全防护 + CORS

**目标**：统一错误处理、路径防护、跨域配置。

**实现步骤**：
1. 在 `main.py` 添加 `@app.exception_handler(Exception)` 全局异常处理，返回 `code=500` + 异常类型名。
2. 添加 `CORSMiddleware(allow_origins=["*"])`（开发态）。
3. 挂载所有路由（`routes_system`、`routes_status`）。
4. 注册 FastAPI 应用启动/关闭事件（数据目录初始化等）。
5. 统一记录 `logger.exception` 日志。

**产出**：`main.py` 应用入口。

**验收点**：未知异常返回 500 JSON 而非裸堆栈，跨域请求可访问。

---

### 任务 1.9　单元测试

**目标**：核心模块测试覆盖。

**实现步骤**：
1. `conftest.py` 提供 `tmp_data_root` fixture（`tmp_path` + monkeypatch `DATA_ROOT`）。
2. `test_store.py`：测试 `write_json`/`read_json` 原子性与 `append_log` 追加。
3. `test_process_manager.py`：`monkeypatch` mock 子进程，测试启停流程与异常退出兜底。
4. 状态转换逻辑单测：`stopped→starting→running→stopping→stopped`、异常退出 `→failed`。
5. `pytest.ini` 或 `pyproject.toml` 配置 `asyncio_mode = auto`（`pytest-asyncio`）。

**产出**：`tests/` 测试用例 + 配置。

**验收点**：`pytest` 全绿。

---

## M2 — 检索/分析/报告/图谱 + 全量 REST

### 任务 2.1　主题检索服务

**目标**：`query` + `source_types` 三方向并行采集并落盘。

**实现步骤**：
1. `search_service.py` 定义 `search(query, source_types)` 主流程。
2. 按 `source_types` 分派到三类采集：公开信息（news）、多媒体（image/video）、内部数据（internal_data）。
3. 用 `asyncio.gather` 并行采集三方向。
4. 生成 `report_id`（主题词拼音缩写 + `YYYYMMDDHHmmss`）。
5. 保存 `SearchRecord`：主题词、执行时间、来源摘要、分析输出到报告元信息。
6. `apps/search_app.py` 作为子进程入口，接收参数执行检索。

**产出**：`services/search_service.py`、`apps/search_app.py`。

**验收点**：`POST /api/search` 返回 `{ report_id, report_md, graph_path }`，数据落盘。

---

### 任务 2.2　内容分析服务

**目标**：观点抽取 + 情绪三分类。

**实现步骤**：
1. `analysis_service.py` 实现 `analyze(texts) -> { sentiments, viewpoints }`。
2. 情绪判定 `rule_based` 模式：正向/负向词典匹配，无命中归中性。
3. 预留 `sentiment_mode` 配置项与 `llm` 模式接口（空实现或桩函数）。
4. 观点抽取：按来源角色聚合核心观点。

**产出**：`services/analysis_service.py`。

**验收点**：测试文本能正确判定正向/中性/负向。

---

### 任务 2.3　洞察生成服务

**目标**：基于分析结果生成洞察。

**实现步骤**：
1. `insight_service.py` 实现 `generate_insights(analysis_result)`。
2. 汇总高频关键词、情绪分布、风险信号。
3. `apps/insight_app.py` 子进程入口。

**产出**：`services/insight_service.py`、`apps/insight_app.py`。

**验收点**：洞察结果写入分析输出并进入报告。

---

### 任务 2.4　报告汇总服务（8 段式）

**目标**：拼装 `report.md` 八段结构。

**实现步骤**：
1. `report_service.py` 实现 `build_report(...)` 拼装八个段落：事件概述、时间线、传播渠道、主要观点、情绪倾向、风险判断、重点证据、结论摘要。
2. 段落「重点证据」带 `source_ref` 引用，与图谱节点/来源关联。
3. 报告头部写元信息（主题词、执行时间、来源摘要）。
4. 写 `reports/{report_id}/report.md`。
5. `apps/report_app.py` 子进程入口。

**产出**：`services/report_service.py`、`apps/report_app.py`。

**验收点**：`report.md` 八段齐全，结论可追溯来源。

---

### 任务 2.5　图谱生成与加载

**目标**：生成并查询 `graph.json`。

**实现步骤**：
1. `graph_service.py` 实现 `generate_graph(report_id, ...)`：构建 `nodes`/`edges`。
2. 节点类型枚举 `topic/person/org/event/source/keyword`，关系类型 `mentions/related_to/published_by/sourced_from`。
3. 每个节点含 `id/type/source_ref`，边含 `source/target/type/source_ref`。
4. 写 `graphs/{report_id}/graph.json`。
5. 实现 `load_graph(report_id)`、`latest_graph()`、`query_graph(node, relation)`。
6. `apps/media_app.py` 子进程入口。

**产出**：`services/graph_service.py`、`apps/media_app.py`。

**验收点**：`graph.json` schema 符合文档 6.4，节点/关系类型枚举正确。

---

### 任务 2.6　图谱接口

**目标**：图谱查询与查看页回退。

**实现步骤**：
1. `routes_graph.py` 实现 `GET /api/graph/latest`。
2. 实现 `GET /api/graph/{report_id}`（不存在返回 `code=404`）。
3. 实现 `POST /api/graph/query`（请求 `{ report_id?, node?, relation? }`）。
4. 实现 `GET /graph-viewer`、`/graph-viewer/{report_id}` 回退到 `index.html`（生产 SPA 托管）。

**产出**：`api/routes_graph.py`。

**验收点**：最新/指定图谱查询正常，按节点/关系过滤正确，404 处理正确。

---

### 任务 2.7　论坛采集服务 + 接口

**目标**：论坛采集启停、日志、历史归档。

**实现步骤**：
1. `forum_service.py` 实现采集任务控制（状态机 `idle/running/stopped/failed`）。
2. 日志写 `runtime/forum/latest.log`（覆盖式最新）。
3. 历史归档 `runtime/forum/history/{date}.json`（数组，条目含 `time/event/message/task_status`）。
4. `apps/forum_app.py` 子进程入口。
5. `routes_forum.py` 实现 `GET /api/forum/start`、`stop`、`log`（`?tail=200`）、`POST /api/forum/log/history`（`{ date }`）。

**产出**：`services/forum_service.py`、`apps/forum_app.py`、`api/routes_forum.py`。

**验收点**：启停正常，最新/历史日志可查，条目四字段齐全。

---

### 任务 2.8　配置服务 + 接口

**目标**：配置读取、更新、热更新、备份。

**实现步骤**：
1. `config_service.py` 实现 `get_config()`、`update_config(new_config)`。
2. 更新前备份 `config.json` → `config.json.bak`。
3. 更新后刷新 `config.py` 缓存，触发运行参数热更新（如 `log_level`）。
4. `routes_config.py` 实现 `GET /api/config`、`POST /api/config`。

**产出**：`services/config_service.py`、`api/routes_config.py`。

**验收点**：更新配置即时生效，生成 `.bak` 备份。

---

### 任务 2.9　接口测试

**目标**：全部 REST 端点测试。

**实现步骤**：
1. `test_api.py` 用 `httpx.AsyncClient` 测试每个端点。
2. 覆盖正常路径与异常路径（参数校验 422、资源不存在 404、重复启动 409）。
3. 测试数据用 `tmp_data_root` fixture 隔离。

**产出**：`tests/test_api.py`。

**验收点**：全部接口测试通过。

---

## M3 — WebSocket 实时通道

### 任务 3.1　WebSocketManager

**目标**：连接管理 + 广播 + 心跳。

**实现步骤**：
1. `ws_manager.py` 维护 `set[WebSocket]` 连接集合。
2. 实现 `connect(ws)`/`disconnect(ws)`/`broadcast(message)`。
3. 实现心跳：每 30s 发送 `{ "type": "heartbeat" }`，60s 无响应主动断开。
4. 消息信封 `{ type, data, ts }` 统一封装。

**产出**：`core/ws_manager.py`。

**验收点**：多连接广播正常，心跳超时断开。

---

### 任务 3.2　后端消息埋点

**目标**：六类实时消息在对应位置触发。

**实现步骤**：
1. `app_status`：`ProcessManager` 状态变更时广播。
2. `app_output`：`_pump` 输出流广播。
3. `forum_log`：论坛日志新增时广播。
4. `system_status`：系统状态变化广播。
5. `graph_ready`：图谱生成完成广播。
6. `error`：各模块异常时广播。

**产出**：各 service/manager 埋点。

**验收点**：六类消息按文档 7.5 字段与时机触发。

---

### 任务 3.3　/ws 路由接入

**目标**：WebSocket 端点挂载。

**实现步骤**：
1. `main.py` 添加 `/ws` WebSocket 路由。
2. `accept()` 后注册到 `WebSocketManager`。
3. 循环接收消息，处理 `heartbeat_ack`。
4. 断开时从集合移除。

**产出**：`main.py` WebSocket 端点。

**验收点**：客户端可建立连接并收发消息。

---

### 任务 3.4　前端 useWebSocket 封装

**目标**：客户端连接、重连、分发。

**实现步骤**：
1. `composables/useWebSocket.ts` 按文档 10.4 实现。
2. `connect()` 建立连接，`onopen` 重置重试计数。
3. `onmessage` 处理 `heartbeat` 回复 `heartbeat_ack`，其余按 `type` 分发到 Pinia Store。
4. `onclose` 指数退避重连（1s、2s、4s…上限 30s，最多 10 次）。
5. 导出 `{ connected, connect, disconnect, send }`。

**产出**：`frontend/src/composables/useWebSocket.ts`。

**验收点**：断线自动重连，消息正确分发。

---

## M4 — 前端 6 页面 + 图谱可视化

### 任务 4.1　前端工程初始化

**目标**：Vite + TS + Naive UI 脚手架。

**实现步骤**：
1. 创建 `frontend/` 工程：`package.json`、`tsconfig.json`、`vite.config.ts`、`index.html`。
2. 安装依赖（文档 13.2）：`vue`、`vue-router`、`pinia`、`axios`、`naive-ui`、`echarts`。
3. `vite.config.ts` 配置 `/api`、`/ws` 代理到 `127.0.0.1:8000`。
4. `main.ts` 挂载 Vue、Pinia、Router、Naive UI。
5. 配置别名 `@` → `src`。

**产出**：可启动的前端工程。

**验收点**：`npm run dev` 启动，代理转发正常。

---

### 任务 4.2　Axios 封装 + 类型定义

**目标**：统一 HTTP 客户端与 TS 类型。

**实现步骤**：
1. `api/index.ts` 创建 Axios 实例，`baseURL` 空（走代理）。
2. 响应拦截器统一处理 `code`，非 0 抛出错误并提示。
3. 封装各接口函数：`getStatus`、`startApp`、`stopApp`、`search`、`forum*`、`config*`、`graph*`、`system*`。
4. `types/index.ts` 定义 API 响应、WebSocket 消息、Store 状态、节点/关系类型。

**产出**：`api/index.ts`、`types/index.ts`。

**验收点**：类型安全，接口封装齐全。

---

### 任务 4.3　Pinia Store

**目标**：四个 Store 与 WS 消息联动。

**实现步骤**：
1. `stores/app.ts`：`apps`（状态映射）、`outputs`，action 处理 `app_status`/`app_output`。
2. `stores/system.ts`：`systemStatus`、`errors`、`resources`，处理 `system_status`/`error`。
3. `stores/forum.ts`：`taskStatus`、`logLines`、`history`，处理 `forum_log`。
4. `stores/graph.ts`：`graphData`、`currentReportId`，处理 `graph_ready`。

**产出**：`stores/` 四个 Store。

**验收点**：WS 消息正确更新对应 Store 状态。

---

### 任务 4.4　控制台首页 + 应用状态卡片

**目标**：应用启停与状态展示。

**实现步骤**：
1. `AppStatusCard.vue`：展示单应用名称、状态徽标、启动/停止按钮。
2. 状态色映射：`running` 绿、`failed` 红、`stopped` 灰、`starting/stopping` 黄。
3. `ConsoleHome.vue`：循环渲染 `AppStatusCard`，展示系统状态与最近输出摘要。

**产出**：`ConsoleHome.vue`、`AppStatusCard.vue`。

**验收点**：页面展示各应用状态，点击按钮可启停。

---

### 任务 4.5　输出查看面板

**目标**：输出/日志滚动展示。

**实现步骤**：
1. `OutputPanel.vue`：接收 `app_name`，展示输出文本。
2. 滚动容器自动跟随底部（有新内容时 `scrollTop = scrollHeight`）。
3. 订阅 `app_output` 消息实时追加。

**产出**：`OutputPanel.vue`。

**验收点**：实时日志流展示，自动滚动到底部。

---

### 任务 4.6　论坛监控页

**目标**：采集启停 + 日志查看。

**实现步骤**：
1. `ForumPage.vue`：启动/停止按钮，当前任务状态。
2. 展示最新日志（订阅 `forum_log`）。
3. 历史日志查询入口（日期选择 → `POST /api/forum/log/history`）。

**产出**：`ForumPage.vue`。

**验收点**：启停正常，最新/历史日志可查。

---

### 任务 4.7　图谱查看页 + GraphCanvas

**目标**：ECharts 力导向图谱渲染。

**实现步骤**：
1. `GraphCanvas.vue`：将 `graph.json` 映射为 ECharts `graph` 系列（文档 10.5 配置）。
2. 节点类型配色分类，`roam` 缩放平移，`emphasis.focus='adjacency'` 高亮相邻。
3. 节点点击弹侧栏显示详情与来源引用。
4. `GraphViewer.vue`：报告下拉切换（`GET /api/graph/{report_id}`），节点/关系查询入口。

**产出**：`GraphViewer.vue`、`GraphCanvas.vue`。

**验收点**：图谱正常渲染，节点点击查看详情，报告可切换。

---

### 任务 4.8　主题检索页

**目标**：检索请求 + 结果展示。

**实现步骤**：
1. `SearchPage.vue`：主题词输入 + 来源类型多选（`source_types`）。
2. 发起 `POST /api/search`。
3. 展示 8 段式报告（渲染 `report_md`），提供来源查看。

**产出**：`SearchPage.vue`。

**验收点**：检索后展示结构化报告。

---

### 任务 4.9　配置管理页 + 系统状态页

**目标**：配置读写 + 系统状态。

**实现步骤**：
1. `ConfigPage.vue`：读取 `GET /api/config` 回显，编辑后 `POST /api/config` 保存。
2. `SystemStatus.vue`：展示整体服务状态、各应用状态、最近错误、资源概况。

**产出**：`ConfigPage.vue`、`SystemStatus.vue`。

**验收点**：配置保存即时生效，系统状态正确展示。

---

### 任务 4.10　SPA 托管回退

**目标**：生产构建由 FastAPI 托管。

**实现步骤**：
1. 前端 `npm run build` 生成 `dist/`。
2. `main.py` 挂载静态文件 `/`。
3. `/`、`/graph-viewer*` 回退到 `index.html`（处理前端路由）。

**产出**：`main.py` 静态托管。

**验收点**：生产环境直接访问 SPA 页面与图谱查看页。

---

## M5 — 示例数据 + 文档 + 测试收尾

### 任务 5.1　示例数据

**目标**：完整主题的三来源数据 + 报告 + 图谱。

**实现步骤**：
1. `examples/` 下准备一个完整主题的新闻、图片/视频、论坛帖、内部数据样本。
2. 生成对应的 `report.md`、`graph.json`、论坛日志样本。
3. 确保示例数据可直接被系统加载演示。

**产出**：`examples/` 示例数据。

**验收点**：示例数据可被系统读取并展示。

---

### 任务 5.2　完整主题分析案例

**目标**：至少一个完整主题案例文档。

**实现步骤**：
1. `docs/示例案例.md` 撰写一个完整主题的检索→分析→图谱→报告全链路案例。
2. 含输入主题、来源数据、分析过程、最终报告、图谱截图说明。

**产出**：`docs/示例案例.md`。

**验收点**：覆盖完整链路，可直接用于演示。

---

### 任务 5.3　API 接口文档

**目标**：接口说明文档与实现一致。

**实现步骤**：
1. `docs/API.md` 按文档 7.4 列出全部接口：方法、路径、请求字段、返回结构、示例。
2. 补充 WebSocket 消息类型说明（文档 7.5）。
3. 核对与最终实现无出入。

**产出**：`docs/API.md`。

**验收点**：文档与代码实现一致。

---

### 任务 5.4　前端测试

**目标**：Store 逻辑与 WS 分发测试。

**实现步骤**：
1. 引入 `Vitest` + `@vue/test-utils`。
2. 测试各 Pinia Store 的 action 逻辑。
3. 测试 `useWebSocket` 的消息分发与重连逻辑。

**产出**：前端测试用例。

**验收点**：测试通过。

---

### 任务 5.5　全链路联调 + 验收核对

**目标**：逐项对照验收标准。

**实现步骤**：
1. 后端启动 → 前端启动 → 全流程走查。
2. 对照文档第 16 节验收标准逐项验证。
3. 修复联调中发现的问题。

**产出**：验收通过的系统。

**验收点**：文档 16 节全部验收项通过。

---

## 依赖关系图

```
M1（骨架+状态接口）
  └─▶ M2（检索/分析/报告/图谱/论坛/配置 + 全量 REST）
        └─▶ M3（WebSocket）──┐
                             ├─▶ M4（前端 6 页面）
                             └─▶ M5（示例/文档/测试，可与 M4 并行）
```

- **M1 → M2** 硬依赖（服务与数据层为基础）。
- **M3 与 M2 尾部、M4 前期** 可部分并行（页面骨架不依赖 WS，仅实时流依赖）。
- **M5 中示例数据与测试** 可与 M4 并行；文档（API.md、案例）需在 M2/M4 稳定后最终对齐。

---

*本清单依据《开发文档.md》与《需求文档.md》整理，可直接作为任务分配与进度跟踪依据。*
