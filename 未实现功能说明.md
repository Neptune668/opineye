# 未真正实现的功能说明（Placeholder / TODO）

> 文档版本：v1.0
> 关联文档：`开发文档.md`（v1.2）
> 维护原则：随各里程碑推进持续更新，凡「占位实现 / 模拟实现 / 待接入」均在此登记，避免误将占位实现当作完成交付。

---

## 1. 说明目的

本项目遵循「离线可运行、可演示」优先原则，部分依赖外部资源或需后续接入的能力，在开发阶段采用**占位（Placeholder）或模拟（Mock）实现**，保证系统可端到端跑通。本文档集中登记这些未真正实现的点，明确其当前行为、影响范围与计划接入任务，供评审与后续开发追踪。

---

## 2. 占位/模拟实现清单

### 2.1 公开信息采集（news / image / video）

| 项 | 说明 |
|----|------|
| 所属任务 | T6 数据采集模块 |
| 当前状态 | **已实现配置化采集框架**：`news`/`image`/`video`/`forum_post` 通过可插拔数据源适配器采集，默认 `file` 类型（读取本地 JSON），可配置为 `http` 类型接入真实数据源 |
| 当前行为 | 默认从 `data/{type}.json` 读取预置数据（离线可用）；真实网络抓取仍未接入 |
| 计划接入 | 在 `config.json` 的 `datasources` 段将类型改为 `http` 并配置 `url`，即可接入真实数据源（RSS/搜索 API/合规媒体接口），不改 `Collector` 接口 |
| 遗留 | 真实公开信息抓取（网络请求、反爬、合规）仍未实现，见 2.5 |

### 2.2 内部数据源（internal_data）

| 项 | 说明 |
|----|------|
| 所属任务 | T6 数据采集模块 |
| 当前状态 | **已实现**（离线真实可用）：从 `data/` 目录读取预置样本数据 |
| 说明 | 这是保证「离线跑通全流程验收」的核心来源，非占位 |

### 2.3 Celery 单功能应用任务体

| 项 | 说明 |
|----|------|
| 所属任务 | T4 进程管理模块 |
| 当前状态 | **已实现**：各 app_name 已绑定真实任务（`topic_search`/`media_search`/`forum_collect`/`insight`/`report`/`graph`），不再使用占位任务 |
| 说明 | `placeholder_task` 保留作兼容，但已不被注册表引用 |

### 2.4 LLM 增强分析

| 项 | 说明 |
|----|------|
| 所属任务 | T12 可选 LLM 增强 |
| 当前状态 | **已实现**：`LLMClient` 双实现（`HttpLLMClient` + `RuleLLMClient`）+ `FallbackLLMClient` 自动回退 |
| 默认行为 | 未配置 `LLM_API_KEY` 时 `available()` 返回 False，自动回退规则模式，离线可运行 |
| 启用方式 | 在 `.env` 配置 `LLM_API_KEY`/`LLM_BASE_URL`/`LLM_MODEL` 后自动启用 LLM 增强（观点聚类） |
| 增强范围 | 当前仅增强「主要观点」聚类；风险判断仍为规则模式（后续可扩展） |
| 接口影响 | 无，`AnalysisOutput` 契约不变 |

### 2.5 公开数据源清单与合规

| 项 | 说明 |
|----|------|
| 当前状态 | **待确认** |
| 说明 | 公开信息采集的具体数据源清单与合规要求尚未确定（对应文档遗留问题） |

### 2.6 情绪分析词典（扩充版）

| 项 | 说明 |
|----|------|
| 所属任务 | T7 检索分析引擎 |
| 当前状态 | **已扩充**：内置情感词表扩充至正向/负向各约 70+ 词，并支持从 `data/sentiment_words.txt` 加载外部词典（前缀 `+`/`-` 表示极性） |
| 局限 | 仍为词典规则模式，对复杂表达、反讽、领域专有情绪词的识别精度有限 |
| 计划增强 | 后续可引入完整情感词典（如 HowNet 情感词）、或由 T12 LLM 判定情绪，替换 `sentiment.py` 子引擎实现 |
| 接口影响 | 无，仅替换 `sentiment` 子引擎内部实现，`AnalysisOutput` 契约不变 |

### 2.7 论坛采集（模拟日志生成）

| 项 | 说明 |
|----|------|
| 所属任务 | T9 论坛模块 |
| 当前状态 | **模拟实现**：`ForumCollector` 完整生命周期（start/stop/log/history）已实现，但采集内容由内置模拟日志生成器定时产出示例事件，不做真实论坛抓取 |
| 原因 | 真实论坛抓取依赖目标站点、需处理反爬/登录/合规，稳定性不可控 |
| 当前行为 | 启动后按配置周期产出模拟论坛日志事件，可正常验证日志查看、历史归档全流程 |
| 计划接入 | 后续接入真实论坛数据源，替换模拟生成器，不改 `ForumCollector` 接口 |

---

## 3. 状态标记约定

代码中占位实现统一使用以下标记，便于检索：

- 注释标记：`# TODO(placeholder)` 或 `# TODO(mock)`
- 日志提示：运行时输出 `placeholder` 关键字日志

---

## 4. 更新记录

| 日期 | 里程碑/任务 | 变更 |
|------|-------------|------|
| 2026-08-26 | T6 | 新增本文档；登记公开信息采集、Celery 任务体、LLM 增强的占位状态 |
| 2026-08-26 | T7 | 登记情绪分析词典为精简实现，说明局限与增强方向 |
| 2026-08-26 | T9 | 登记论坛采集为模拟日志生成实现 |
| 2026-08-26 | T12 | LLM 增强实现完成，登记默认规则回退与启用方式 |
| 2026-08-26 | 收尾 | 落实 2.1（配置化数据源框架）、2.3（真实任务绑定）、2.6（情绪词典扩充） |
