# 未真正实现的功能说明（Placeholder / TODO）

> 文档版本：v2.0
> 关联文档：`需求文档.md`、`开发文档.md`（v1.2）
> 维护原则：随各里程碑推进持续更新，凡「占位实现 / 模拟实现 / 待接入 / 需求缺口」均在此登记，避免误将占位实现当作完成交付。

---

## 1. 说明目的

本文档集中登记项目中**尚未真正实现**的功能点，明确其当前行为、影响范围与计划接入方式，供评审与后续开发追踪。已实现的功能不再登记于此。

---

## 2. 未实现功能清单

### 2.1 需求缺口（接口/契约缺失）

| 编号 | 未实现项 | 需求出处 | 当前行为 | 说明 |
|------|----------|----------|----------|------|
| G1 | `GET /graph-viewer` | 需求 2.2.9 | 未提供该独立路径 | 前端当前用 SPA 内部路由切换图谱页，未暴露需求要求的 `/graph-viewer` 独立路由 |
| G2 | `GET /graph-viewer/{report_id}` | 需求 2.2.9 | 未提供该独立路径 | 同上，需提供按 report_id 直达图谱查看页的路由 |
| G3 | `app_output` 实时消息未实际发布 | 需求 2.2.10 | 仅定义事件类型，应用无输出事件触发发布 | 需在应用产生输出时发布 `app_output` 消息 |
| G4 | `graph_ready` 实时消息未实际发布 | 需求 2.2.10 | 仅定义事件类型，图谱生成后未发布 | 需在图谱生成完成时发布 `graph_ready`（含 report_id、graph_path） |
| G5 | `error` 实时消息未实际发布 | 需求 2.2.10 | 仅定义事件类型，异常未走事件发布 | 需在模块异常时发布 `error`（含 module_name、error_message） |

### 2.2 数据完整性缺口

| 编号 | 未实现项 | 需求出处 | 当前行为 | 说明 |
|------|----------|----------|----------|------|
| D1 | 应用标准/错误输出未写入 `runtime/apps/{app_name}.log` | 需求 2.2.4、2.2.7 | 该文件路径已约定，但单功能应用执行时未实际写入 stdout/stderr | 需在任务执行时捕获并写入对应日志文件 |
| D2 | 报告结论未显式绑定来源 ID | 需求 2.2.8 | 有 `ref` 字段，但「结论摘要」未显式引用来源/图谱节点 ID | 需在结论中增加来源追溯标识 |

### 2.3 能力缺口

| 编号 | 未实现项 | 需求出处 | 当前行为 | 说明 |
|------|----------|----------|----------|------|
| C1 | 角色鉴权（RBAC） | 需求 2.2.2 | 定义了 3 种角色（操作用户/报告查看人/系统管理员），但接口未按角色鉴权 | 已预留 token 鉴权中间件，前端阶段接入 |
| C2 | 输出模块滚动展示 | 需求 2.2.5 | 有日志查看，但无自动滚动到最新 | 前端增强项，优先级低 |

### 2.4 外部依赖（需提供数据源才能落地）

| 编号 | 未实现项 | 当前行为 | 计划接入 |
|------|----------|----------|----------|
| E1 | 论坛真实抓取 | 论坛采集由内置模拟日志生成器产出示例事件，不做真实论坛抓取 | 需提供目标论坛站点后，接入真实数据源，替换模拟生成器，不改 `ForumCollector` 接口 |
| E2 | 公开数据源清单与合规 | 公开信息采集已通过 Tavily 实现，但具体数据源清单与合规要求未正式确认 | 待确认数据源清单与合规要求 |

---

## 3. 状态标记约定

代码中占位实现统一使用以下标记，便于检索：

- 注释标记：`# TODO(placeholder)` 或 `# TODO(mock)`
- 日志提示：运行时输出 `placeholder` 关键字日志

---

## 4. 更新记录

| 日期 | 里程碑/任务 | 变更 |
|------|-------------|------|
| 2026-08-26 | T6~T12 | 各任务实现过程登记（详见历史版本） |
| 2026-08-26 | 收尾 | 落实配置化数据源、真实任务绑定、情绪词典扩充、Tavily 接入、MySQL/LLM 生效 |
| 2026-08-26 | 需求复核 | 清理已实现项，新增需求缺口（graph-viewer、3 类实时消息、应用日志、RBAC、结论追溯、滚动展示） |
